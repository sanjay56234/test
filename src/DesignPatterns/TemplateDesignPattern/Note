It is required to given the function in our code,
so that the base classes should follow the steps in same order,
but also it should have the functionality to change the implementation of each step a/c to them